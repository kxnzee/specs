<!--
Design — техническое решение для читателя, которому недоступны история беседы
и внутреннее устройство Code Repositories. Пиши по-русски; сохраняй точные имена
публичных полей, адреса и форматы протоколов. Объём определяется сложностью решения.
Это единый источник правил содержания и проверки Design для этой schema.

Опора решения — принятые ограничения Proposal и Requirements/Scenarios Delta Specs.
Ссылайся на их точные заголовки/ID, не пересказывай требования целиком.
Если Specs ещё не созданы, Design может быть черновиком на основе Proposal;
не объявляй его проверенным для Tasks до сверки с необходимыми Specs. При явно
разрешённом skip_specs опирайся на принятые критерии Proposal, не выдумывай Specs.
Не ссылайся на Intake, его пункты или историю интервью. Позднее уточнение поведения
сначала согласуется с владельцем и отражается в Specs; затем обеспечивается Design.
Обратная актуализация Intake выполняется отдельно и не становится содержанием Design.

Разрешены архитектура, публичные API, схемы данных, примеры сообщений, диаграммы,
адреса взаимодействий, ответственность Repository IDs и проверяемые решения.
Не переноси внутренние файлы и path:line, классы, функции, константы, выражения,
выбранные внутренние библиотеки/таблицы, команды сборки, тестовый код или план
построчных изменений. Исследование кода подтверждает текущее состояние, но его
отчёт остаётся в результате агента. Не переноси эти детали в Tasks взамен Design.
JSON/XML/OpenAPI и примеры запросов допустимы как публичный контракт; фрагмент
реализации на Java/TypeScript или псевдокод алгоритма им не является.
Не добавляй PR, Git revisions, историю запусков, служебные статусы или новые файлы
отчёта. Ссылки на нормативную API-спецификацию и адреса API не являются PR-ссылками.

Перед завершением проверь каждое решение по требованиям, каждый изменяемый контракт
по разделу ниже, применимость evidence, совместимость и откат. Удаляй повторы:
один факт имеет один основной раздел. Неприменимость объясняй, неизвестное сохраняй
как вопрос. Неполный обязательный контракт, ссылка на Intake, внутренняя реализация
или противоречие требованиям — blocker. Открытым можно оставить только вопрос,
который не меняет контракт, подход или разбиение Tasks. Удаляй подсказки из результата.
-->

## Context

<!-- Только текущее состояние и ограничения, влияющие на подход. Мотивация — в Proposal. -->

## Goals / Non-Goals

**Goals:**
<!-- Какие технические результаты обеспечивают принятые требования. -->

**Non-Goals:**
<!-- Только дополнительные технические границы; не копируй scope Proposal. -->

## Architecture and interactions

<!--
Покажи участников, границы ответственности и путь данных. Для межкомпонентного
изменения добавь диаграмму потока/последовательности в формате PlantUML
(блок `plantuml` с `@startuml` и `@enduml`), не Mermaid: публичные сообщения,
основной сценарий и значимые отклонения. При проверке Design проверь и формат
диаграммы. Не называй внутренние классы и функции.
Для локального изменения без межкомпонентного обмена объясни неприменимость.
-->

## Interaction / API contracts

<!--
Для каждого затронутого публичного взаимодействия опиши существующий и целевой
контракт: что добавляется, меняется, удаляется или остаётся прежним.

- Назначение, производитель и потребители, связь с Requirement/Scenario.
- Протокол, операция и точный адрес или параметризованный шаблон адреса.
  HTTP: метод + URL (либо base URL и path), path/query-параметры и значимые headers.
  Событие: broker/service, topic/channel, тип события и ключ маршрутизации, если есть.
  XMPP: JID-адресация, тип stanza, element и namespace URI. Укажи, откуда берутся
  параметры адреса. Не выдумывай домен окружения, endpoint или HTTP-метод для XMPP.
- Запрос/ответ либо сообщение: поля, типы, обязательность, ограничения, единицы,
  отсутствие/null/пустые значения и значения по умолчанию, когда применимо.
  При изменении поля покажи его место в существующей структуре, не только имя.
- Короткий пример сообщения или запроса/ответа; обозначь условные значения.
- Доступ, ошибки, порядок/повторная доставка или таймауты — по применимости.
  Сохраняемые правила называй прямо; не добавляй неподтверждённые гарантии.
- Совместимость старых и новых участников, версия контракта и миграция при необходимости.

Адреса, namespace, форматы и существующее поведение берутся из подтверждённых
источников. Новое проектируемое значение явно обозначается как решение Change.
Если обязательная часть неизвестна, назови пробел и не объявляй контракт готовым.
При отсутствии изменений публичных взаимодействий достаточно обоснования применимости.
-->

## Decisions

<!-- Решение, его основание в Proposal/Specs, рассмотренная альтернатива и причина выбора. -->

## Repository Implementation Map

| Repository | Responsibility | Contracts and dependencies |
| --- | --- | --- |
| `<repository-id>` | <!-- результат в принятой области Change --> | <!-- публичные границы и ссылки на контракты выше --> |

## Risks / Trade-offs

<!-- Риск, его последствия и способ ограничения; отделяй подтверждённое от предположений. -->

## Migration, rollout and rollback

<!--
Что требуется для введения изменения; поведение смешанных версий; какие компоненты
можно откатить и что это реально меняет. Не утверждай симметричный откат без основания:
откат проверки или неизменённого посредника не отменяет поведение производителя.
Для неприменимых миграций достаточно причины; не придумывай процесс поставки проекта.
-->

## Open questions

<!-- Только допустимые отложенные вопросы; если их нет, напиши, что отсутствуют. -->
